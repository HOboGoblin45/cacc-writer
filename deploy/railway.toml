[build]
builder = "nixpacks"

[deploy]
startCommand = "node cacc-writer-server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.envvars]
NODE_ENV = "production"
PORT = "5178"
